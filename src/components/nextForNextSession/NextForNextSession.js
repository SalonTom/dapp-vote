function NextForNextSession() {
    return <div style={{ width: "100vw", height: "100vh", display: "flex", alignItems: "center", justifyContent: "center"}}>
        <div>Wait for next session.</div>
    </div>
}

export default NextForNextSession;